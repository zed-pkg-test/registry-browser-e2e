[package]
org = "zed-pkg-test"
name = "registry-browser-e2e"
version = "0.1.0"
description = "Independent browser acceptance harness"
license = "MIT"

[package.repository]
vcs = "git"
url = "https://github.com/zed-pkg-test/registry-browser-e2e"

[install]
dir = ".vendor/.zed"

[publish]
exclude = ["artifacts/**", "vendor/**", ".vendor/**", ".env", ".env.*"]
